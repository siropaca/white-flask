pre-commit:
  parallel: true
  commands:
#    lint:
#      glob: "*.{js,ts,json}"
#      run: pnpm run lint:fix
    type-check:
      glob: "*.{ts,tsx}"
      run: pnpm run type-check

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
